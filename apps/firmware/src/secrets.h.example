#ifndef SECRETS_H
#define SECRETS_H

// Wi-Fi
#define WIFI_SSID     ""
#define WIFI_PASSWORD ""

// MQTT broker (static IP/hostname — recommended). Leave empty to fall back
// to subnet auto-discovery.
#define MQTT_SERVER   ""
#define MQTT_USER     "esp32_front_door"
#define MQTT_PASS     ""

#endif
